#include "stdafx.h"
#include "ConfMat.h"

using namespace std;

class ShowMatches
{
public:
	//ShowMatches();
	//~ShowMatches();
	void showMatches(ConfMat x, const vector <vector <vector<DMatch>>> &matches);

private:

};
